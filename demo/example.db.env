# Application environment: set to "development" for mock data
# or "production" for real database connection
#APP_ENV=development

# Database connection parameters
# Only used when APP_ENV is not "development" or when explicitly needed
DB_HOST=local.hasura.dev
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=password
DB_NAME=postgres
